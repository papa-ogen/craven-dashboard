import React from 'react'

// TODO: Implement For

type ForProps = {
  each: Array<any>
}
{
  /* <For each={links}>
            {link => (
              <li className="pb-4">
                <Link link={link} />
              </li>
            )}
          </For> */
}

const For = ({ each }: ForProps) => {
  return <div>For</div>
}

export default For
